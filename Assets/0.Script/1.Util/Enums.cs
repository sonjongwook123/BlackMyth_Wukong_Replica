using System;

public static class Enums
{
    public enum Sfx
    {
        None = -1,
        ChAttack0 = 0,
    };

    public enum Bgm
    {
        None = -1,
        BattleMusic = 0,
        
    };
}